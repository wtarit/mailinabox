[project]
name = "mailinabox-duplicity"
version = "0.0.0"
description = "Locked, isolated duplicity runtime for Mail-in-a-Box"
requires-python = ">=3.14,<3.15"
dependencies = [
	"duplicity==3.1.0",
]

[tool.uv]
package = false
required-version = "==0.11.30"
